// @ts-nocheck
import React from "react";
import { oceanControlVariantLabel } from "../../lib/gameEngines/oceanControlEngine";
const BLUE="#30b9ff",CYAN="#65e9ff",GOLD="#f5ca68",RED="#ff6573",SOFT="#aab4c7"; const n=(v:any)=>Number.isFinite(Number(v))?Number(v):0;
export default function OceanControlHistoryScoreBlock({record}:any){const variant=record?.summary?.variant||record?.payload?.variant||record?.payload?.config?.variant||"tactical";const rows=record?.summary?.rankings||record?.summary?.perPlayer||record?.payload?.stats?.players||record?.players||[];return <div style={{marginTop:8,display:"grid",gap:5}}><div style={{color:CYAN,fontSize:9,fontWeight:1100}}>⚓ {oceanControlVariantLabel(variant)}</div>{(Array.isArray(rows)?rows:[]).slice().sort((a:any,b:any)=>n(a?.rank)-n(b?.rank)).map((row:any,index:number)=><div key={row?.id||index} style={{display:"grid",gridTemplateColumns:"28px minmax(0,1fr) auto auto",gap:6,alignItems:"center",padding:7,borderRadius:11,background:"rgba(255,255,255,.035)",border:"1px solid rgba(255,255,255,.08)"}}><strong style={{color:n(row?.rank)===1?GOLD:SOFT}}>#{n(row?.rank)||index+1}</strong><span style={{whiteSpace:"nowrap",overflow:"hidden",textOverflow:"ellipsis",fontWeight:950}}>{row?.name||"Joueur"}</span><strong style={{color:RED}}>{n(row?.shipsSunk)} nav.</strong><span style={{color:BLUE,fontSize:8}}>{n(row?.shipHits)} hits</span></div>)}</div>}
